export const arrivalSelector = state => state.flights.arrival;
export const departureSelector = state => state.flights.departure;

export const searchingValueSelector = state => state.searching.searchInfo;

export const searchArrivalSelector = state => state.searching.searchArrival;
export const searchDepartureSelector = state => state.searching.searchDeparture;
